import React from 'react';
import {View, Text} from 'react-native';

interface Props {}

const Home = (props: Props) => {
  return (
    <View>
      <Text>Home Page</Text>
    </View>
  );
};

export default Home;
